services:
  redis:
    restart: always
    image: redis:latest
  db:
    restart: always
    image: postgres:latest
    ports:
      - 5432:5432
    env_file:
      .env
    volumes:
      - postgres_data:/var/lib/postgresql/data/
  django:
    restart: always
    build: .
    command: /home/knotify_webserver/docker-entrypoint.prod.sh
    volumes:
      - .:/home/knotify_webserver
    ports:
      - 8000:8000
    depends_on:
      - db
      - redis
      - knotify
    env_file:
      .prod_env
  knotify:
    restart: always
    build:
      context: ./knotify/
    command: celery -A tasks worker -l INFO --max-tasks-per-child=1
    mem_limit: 512m
    depends_on:
      - redis
  nginx:
    build: ./nginx
    ports:
      - 80:80
    volumes:
      - .:/home/knotify_webserver
    depends_on:
      - django
volumes:
  postgres_data: